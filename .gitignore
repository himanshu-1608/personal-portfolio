# Python cache
__pycache__/
*.pyc
*.pyo

# Input folder — raw broker CSVs (private/sensitive, never push)
input/*.csv
input/*.xlsx
input/*.xls

# Personal/local notes
CLAUDE.md
context.md
learnings.md

# Zerodha automation — secrets, raw daily downloads, browser profile, logs
python/*.env
zerodha-daily-reports/
.zerodha-browser/
logs/

# Local-only run scripts (contain absolute local paths / machine-specific) — never push
shell/*.plist
shell/nightly_sync.sh

.claude/